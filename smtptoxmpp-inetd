service smtptoxmpp
{
	type		= UNLISTED
	socket_type	= stream
	protocal	= tcp
	port 		= smtp
	wait		= no
	user		= nobody
	server		= /usr/sbin/smtptoxmpp
	server_args	= /etc/smtptoxmpp.conf
	disable		= no
}
